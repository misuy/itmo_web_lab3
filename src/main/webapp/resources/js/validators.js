function validate_x() {
    if (!isNaN(parse_x_value())) {
        console.log("x is OK")
    }
}

function validate_y() {
    if (!isNaN(parse_y_value())) {
        console.log("y is OK")
    }
}

function validate_r() {
    if (!isNaN(parse_r_value())) {
        console.log("r is OK")
    }
}